# Security: do not run package lifecycle scripts (postinstall, preinstall, etc.)
# on install. This blocks the most common npm supply-chain attack vector —
# malicious install scripts that inject code into your project files.
# If a package legitimately needs its install script, run it deliberately with
# `npm rebuild <pkg>` or temporarily set ignore-scripts=false.
ignore-scripts=true
